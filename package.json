{
  "name": "spurtalk",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "backend",
    "shared",
    "types",
    "web",
    "mobile"
  ],
  "scripts": {
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "db:migrate": "prisma migrate dev",
    "db:push": "prisma db push",
    "db:seed": "prisma db seed",
    "db:studio": "prisma studio",
    "db:generate": "prisma generate",
    "dev": "concurrently \"npm run dev --workspace=backend\" \"npm run dev-custom --workspace=web\"",
    "dev-custom": "concurrently \"npm run dev --workspace=backend\" \"npm run dev-custom --workspace=web\"",
    "stop": "pkill -f \"tsx watch src/index.ts\" && pkill -f \"node server.js\" || echo 'No processes found to stop'",
    "restart": "npm run stop && npm run dev-custom",
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "devDependencies": {
    "@types/react-test-renderer": "^19.0.0",
    "@typescript-eslint/eslint-plugin": "^6.21.0",
    "@typescript-eslint/parser": "^6.21.0",
    "concurrently": "^8.2.2",
    "eslint": "^8.56.0",
    "eslint-config-prettier": "^10.1.8",
    "prettier": "^3.1.1",
    "prisma": "^5.8.0",
    "react-test-renderer": "^19.2.3",
    "typescript": "^5.3.3"
  },
  "prisma": {
    "schema": "./prisma/schema.prisma"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.985.0",
    "@aws-sdk/lib-dynamodb": "^3.985.0",
    "@prisma/client": "^5.22.0"
  }
}
